kind: PersistentVolume
apiVersion: v1
metadata:
  name: shared-vol
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/shared/motion"

---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: shared-vol
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---

apiVersion: apps/v1 
kind: Deployment
metadata:
    name: image-video

spec:
  replicas: 1
  selector:
    matchLabels:
      app: image-video
  template:
    metadata:
      labels:
        app: image-video

  
    spec:
      # Add the ConfigMap as a volume to the Pod
      volumes:
      - name: shared-vol
        persistentVolumeClaim:
          claimName: shared-vol
      containers:
      - name: image-to-video
        image: localhost:32000/image-video
      # Mount the volume that contains the configuration data 
      # into your container filesystem
        volumeMounts:
        - name: shared-vol
          mountPath: /shared
