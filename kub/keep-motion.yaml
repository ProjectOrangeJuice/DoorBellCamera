kind: PersistentVolume
apiVersion: v1
metadata:
  name: shared-vol
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/shared/motion"

---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: shared-vol
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---

apiVersion: apps/v1 
kind: Deployment
metadata:
    name: keep-motion

spec:
  replicas: 1
  selector:
    matchLabels:
      app: keep-motion
  template:
    metadata:
      labels:
        app: keep-motion

  
    spec:
      # Add the ConfigMap as a volume to the Pod
      volumes:
      - name: shared-vol
        persistentVolumeClaim:
          claimName: shared-vol
      containers:
      - name: check-motion
        image: localhost:32000/keep-motion
      # Mount the volume that contains the configuration data 
      # into your container filesystem
        volumeMounts:
        - name: shared-vol
          mountPath: /shared
